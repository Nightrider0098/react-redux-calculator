const initialState = {
    number: 0, //number what is displayed on screen
    storedFunc: (x) => x, //function for operator +/*-
    lastAction: undefined //type of action performed
}
module.exports = initialState